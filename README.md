# Simple GIF Editor

macOS 専用のローカル完結な GIF 編集アプリです。Kap (スクリーン録画ソフト) で録画した GIF を入力として、時間トリミング、矩形クロップ、矩��モザイク、フレーム削減を行い、GIF として書き出します。

## 背景

ScreenToGif で行��ていた作業を、Kap + 本アプリで macOS 上で完結させることを目的としています。ScreenToGif ��完全クロ���ンではなく、「自分用の Kap companion」と��て成立する最小機能を備えています。

## 機能

- **GIF 読み込み**: Kap が出力した GIF ファイルを読み込み、即座にフレームを抽出します
- **GIF プレビュー**: `<img>` 要素による GIF のループ再生
- **時間トリミング**: in/out ポイントをドラッグで設定し、出力範囲を制限���ます
- **矩形クロップ**: 出力する矩形範囲を指定します (エクスポート時に適用)
- **矩形モザイク**: 指定した時間範��のフレームに対し、矩形領域をピクセル化します。ブ��ックの平均色で塗りつぶす���式です。複数回の適用が可能です
- **フレーム削減**: 全フレ���ムに対して均等に間引きます (1/2、1/4、1/8)。再生速度は維持されます
- **Undo**: 直前 1 回の破壊的操作 (モ���イク・フレーム削減) を元に戻せます
- **GIF 書き出し**: palettegen/paletteuse によ���デフォルト 10fps の GIF を生成します

## セットアップ

### 前提条件

- macOS (Apple Silicon)
- Node.js 24 以上
- npm

### イン���トール

```bash
npm install
```

`postinstall` スク���プトによ���、sharp のネイティブモジュー���が Electron 向けに自動的��リビルドされます。

### 開発モードでの起動

```bash
npm run build
npm start
```

### テスト

```bash
npm test
```

テスト実行時���、テストフィクスチャ (サンプル GIF, PNG) が `.tmp/test-fixtures/` に自動生成されます。

```bash
npm run test:coverage
```

### パッケージング

```bash
npm run package
```

`out/` ディレクトリに macOS ���の .app バンドルが生成されます。

## 使い方

1. アプリを起動しま���
2. 「Open GIF」をクリックし、Kap で録画した GIF を選択します
3. タイムラ��ンのハンドルをド��ッグして、トリミング範囲を設定します
4. 「Draw Crop」をクリックし、プレビュー上でクロップ矩形を��画します
5. 「Draw Mosaic」をクリックし、モザイク矩形を描画して時間範囲を指定し「Apply」をクリックします
6. フレーム削減が必要な場合は「1/2」「1/4」「1/8」をクリックし���す
7. 「Export GIF」をク��ックして GIF ���書き出します

## 技術構成

| 分類 | 技術 |
|------|------|
| アプ��シェル | Electron |
| 言語 | TypeScript (strict) |
| レンダラバンドラ | Vite |
| GIF/画像��理 | FFmpeg (ffmpeg-static でバンドル) |
| モザイク処理 | sharp |
| ���スト | Vitest |
| パッケージング | electron-builder |

## 制約

- macOS 専用です (Windows/Linux には対応していません)
- 入力は GIF のみです
- 出力は GIF のみです
- モザイクとフレーム削減は破壊的操���です (Undo は直前 1 回のみ)
- フレーム抽出により一時的にディスク容量を消費します

## 拡張ポ���ント

- モ���イクの時間範囲を UI ���でドラッグ指定する機能
- GIF 品質パラ���ータ (fps、色数) の設定 UI
- 複数ファイルの連続処理
- MP4 入力への対応

## 更新情報

### v0.2.0

- 入力フォーマットを MP4 から GIF に変更
- プレビュー方式を `<video>` から `<img>` に変更
- ファイルオープン時に即座にフレーム抽出を実行するフローに変更

### v0.1.0

- 初回リリース (MVP)
- GIF 読み込み、プレビュー、時間トリミング、矩形クロップ
- 矩形モザイク (時間範囲指定��破壊的適用)
- フレーム削減 (1/2、1/4、1/8)
- Undo (直前 1 回)
- GIF 書き出し (palettegen/paletteuse、デフォルト 10fps)
